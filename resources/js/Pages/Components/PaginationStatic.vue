<template>
    <!-- <div class="flex justify-between items-center mt-5">
    <p class="text-xl">Showing {{ users.from }} to {{ users.to }} of {{ users.total }} entries</p>
    <div>
        <Link v-for="page in users.links" 
        :key="page.url" 
        :href="page.url" 
        v-html="page.label"
        class="mt-5 px-4 py-2 inline-block mx-1"
        :class="[
            {'bg-blue-500 text-white': page.active, 'bg-gray-200': !page.active},
            {'!bg-slate-100 text-slate-400': !page.url}
        ]"
    />
    </div>
</div> -->
</template>